# qa 프로필 로깅 — QA 검증에 필요한 정보만. logback-spring.xml 의 <springProfile name="qa"> 가
# 한 줄 구조화 패턴 + 축약 스택(%rEx{8})을 담당. 여기서는 로거별 레벨만.
# payload(req/resp 본문) 로거는 명시하지 않으므로 INFO 기준선에서 자동 억제된다.
logging:
  level:
    root: INFO
    com.crosscert.passkey: INFO
    org.springframework.web: WARN
